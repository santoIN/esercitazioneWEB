CREATE TABLE calciatori (
                            id INT AUTO_INCREMENT PRIMARY KEY,
                            nome VARCHAR(100),
                            squadra VARCHAR(100),
                            gol INT
);
